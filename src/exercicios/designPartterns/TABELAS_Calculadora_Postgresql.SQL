-- Drop table

-- DROP TABLE PUBLIC.EXPRESSOES;

CREATE TABLE PUBLIC.EXPRESSOES (
	ID CHARACTER VARYING(255) NOT NULL,
	EXPRESSAO CHARACTER VARYING(255),
	CONSTRAINT CONSTRAINT_B PRIMARY KEY (ID)
);
--CREATE UNIQUE INDEX PRIMARY_KEY_B ON PUBLIC.EXPRESSOES (ID);

-- PUBLIC.OPERACOES definition

-- Drop table

-- DROP TABLE PUBLIC.OPERACOES;

CREATE TABLE PUBLIC.OPERACOES (
	ID CHARACTER VARYING(255) NOT NULL,
	OPERACAO bytea,
	CONSTRAINT CONSTRAINT_9 PRIMARY KEY (ID)
);
--CREATE UNIQUE INDEX PRIMARY_KEY_9 ON PUBLIC.OPERACOES (ID);

-- PUBLIC.RESULTADOS definition

-- Drop table

-- DROP TABLE PUBLIC.RESULTADOS;

CREATE TABLE PUBLIC.RESULTADOS (
	ID CHARACTER VARYING(255) NOT NULL,
	RESULTADO INTEGER,
	CONSTRAINT CONSTRAINT_F PRIMARY KEY (ID)
);
--CREATE UNIQUE INDEX PRIMARY_KEY_F ON PUBLIC.RESULTADOS (ID);


SELECT * FROM PUBLIC.operacoes o  

truncate expressoes;
truncate operacoes;
truncate resultados;